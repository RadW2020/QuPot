{
  "name": "@qupot/common-lib",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "format": "prettier --write \"src/**/*.ts\"",
    "lint": "eslint \"{src,apps,libs}/**/*.ts\" --fix",
    "dev": "npm run build -- --watch",
    "clean": "rm -rf .turbo node_modules dist"
  },
  "devDependencies": {
    "@types/node": "^20.3.1",
    "eslint": "^8.42.0",
    "tsup": "^7.2.0",
    "typescript": "^5.1.3"
  }
}
